# Core dependencies
fastapi[standard]>=0.115.0
fastapi-cli>=0.0.7
uvicorn[standard]>=0.27.1
python-multipart>=0.0.9
pydantic>=2.9.0
pydantic-settings>=2.4.0

# Database
sqlalchemy[asyncio]>=2.0.25
asyncpg>=0.30.0
psycopg2-binary>=2.9.9
alembic>=1.13.1
greenlet>=3.1.0

# Redis
redis>=5.0.1

# Google OAuth (direct verification, no Firebase)
google-auth>=2.27.0
google-auth-httplib2>=0.2.0

# AWS S3 compatible (for R2)
boto3>=1.34.34

# HTTP client
httpx>=0.26.0
aiohttp>=3.10.0

# Google Gemini AI
google-generativeai>=0.8.3

# Security - JWT + password hashing
python-jose[cryptography]>=3.3.0
passlib[bcrypt]>=1.7.4

# Utilities
python-dateutil>=2.8.2
orjson>=3.10.0

# Media processing
Pillow>=10.4.0

# Testing
pytest==8.0.0
pytest-asyncio>=0.23.5
pytest-cov==4.1.0

# Dev tools
ruff==0.2.1
mypy==1.8.0
pre-commit==3.6.0

# Missing for Pydantic EmailStr validation
email-validator>=2.0.0

# Recommended for production performance (Linux/Docker only)
uvloop==0.19.0; sys_platform != 'win32'
watchfiles>=0.24.0
